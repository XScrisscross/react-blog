# 123

```jsx
import React from 'react'
import MD from '../../source/books/sectionD/1-搭建自己的(一).mdx'

export default class BookContent extends React.Component {
  render() {
    return (
      <div>
        <MD />
      </div>
    )
  }
}

```

```
<template>
  <div class="wraper-detail">
    <div class="header">
      <x-tab-card :rawData="tabList" :current="current" @tabAction="tabHandle" key="header"></x-tab-card>
    </div>
    <div class="container">
      <x-scroll-bar>
        <div class="content">
          <leader-portal-detail-rs v-if="current === 0"></leader-portal-detail-rs>
          <leader-portal-detail-kq v-if="current === 1"></leader-portal-detail-kq>
          <leader-portal-detail-jx v-if="current === 2"></leader-portal-detail-jx>
        </div>
      </x-scroll-bar>
    </div>
  </div>
</template>

<script>
import { mapState, mapActions, mapMutations, mapGetters } from 'vuex'

export default {
  components: {
    LeaderPortalDetailRs: () => import('./LeaderPortalDetailRs'),
    LeaderPortalDetailKq: () => import('./LeaderPortalDetailKq'),
    LeaderPortalDetailJx: () => import('./LeaderPortalDetailJx'),
  },
  data () {
    return {
      tabList: ['人事', '考勤', '绩效考核'],
      current: 1
    }
  },
  watch: {
    $route: {
      handler () {
        this.initComponent()
      },
      deep: true
    }
  },
  created () {
    this.initComponent()
  },
  methods: {
    initComponent () {
      // init-route
      this.current = Utils_String.transErrorsTo(this.$route.query.current, 0)

      // 请求所有数据至store
      this.fetchDataToStore()
    },

    // handle
    tabHandle (val) {
      const { index } = val
      // console.log('tabHandle', index);
      this.current = Number(index)
    },

    // renders
    fetchDataToStore () {
    },
  },
}
</script>

<style lang="scss" scoped>
@import '../../assets/style/index.scss';

.wraper-detail {
  width: 100%;
  height: 100%;
  padding: 20px;
  background-color: $background;
  display: flex;
  flex-direction: column;

  .header {
    border-bottom: 1px solid $liner;
  }

  .container {
    flex: auto;
    margin-top: 20px;
    height: calc(100% - 80px);
    border-radius: 5px;
    background-color: $background;

    .content {
    }
  }
}
</style>
```

